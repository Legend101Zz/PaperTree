// @papertree/db — SQLite data layer for PaperTree v2.
//
// Placeholder so the package typechecks and lints while F0.5 is in flight.
// F0.5 replaces this file with the real surface: the migration runner and the
// owner-scoped query helpers (see research/build/EPIC-00-spine.md, db/ownership.spec).
export const PACKAGE_NAME = '@papertree/db';
